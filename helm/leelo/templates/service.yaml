apiVersion: v1
kind: Service
metadata:
  name: {{ include "leelo.fullname" . }}
  labels:
    {{- include "leelo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "leelo.selectorLabels" . | nindent 4 }} 